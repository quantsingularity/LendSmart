# Global .gitignore for LendSmart Project

# General
.DS_Store
*.log
*.tmp
*.swp
*.swo

# IDE specific
.idea/
.vscode/
*.iml
*.project
*.classpath

# Build artifacts
build/
dist/

# Node.js / JavaScript / TypeScript
node_modules/
npm-debug.log
yarn-error.log
package-lock.json # Often committed, but can be ignored in some monorepo setups if using a single lock file at root
# yarn.lock # Same as above
*.tsbuildinfo

# Python
__pycache__/
*.py[cod]
*.egg-info/
.env
.venv/
venv/
ENV/
env/
# Jupyter Notebook checkpoints
.ipynb_checkpoints/

# Operating System files
Thumbs.db
Desktop.ini

# Sensitive files
*.env
.env.*
!/.env.example # Keep example env files

# Specific to sub-projects (can also be in their own .gitignores)

# Backend (Node.js)
LendSmart/backend/node_modules/
LendSmart/backend/.env
LendSmart/backend/coverage/

# Smart Contracts (Hardhat)
LendSmart/smart-contracts/node_modules/
LendSmart/smart-contracts/.env
LendSmart/smart-contracts/cache/
LendSmart/smart-contracts/artifacts/
LendSmart/smart-contracts/coverage/
LendSmart/smart-contracts/coverage.json
LendSmart/smart-contracts/typechain-types/

# ML Model (Python)
LendSmart/ml-model/venv/
LendSmart/ml-model/.env
LendSmart/ml-model/__pycache__/
LendSmart/ml-model/saved_models/*.joblib # Model files can be large, consider Git LFS or not committing them
LendSmart/ml-model/saved_models/*.pkl
LendSmart/ml-model/processed_data/*.csv # Processed data can also be large
LendSmart/ml-model/.ipynb_checkpoints/

# Mobile Frontend (React Native)
LendSmart/mobile-frontend/node_modules/
LendSmart/mobile-frontend/.env
LendSmart/mobile-frontend/ios/build/
LendSmart/mobile-frontend/ios/Pods/
LendSmart/mobile-frontend/android/build/
LendSmart/mobile-frontend/android/app/build/
LendSmart/mobile-frontend/android/.gradle/
LendSmart/mobile-frontend/android/captures/
LendSmart/mobile-frontend/.expo/

# Web Frontend (React)
LendSmart/web-frontend/node_modules/
LendSmart/web-frontend/.env
LendSmart/web-frontend/build/
LendSmart/web-frontend/coverage/

# If using a global lock file at the root for a monorepo (e.g. with pnpm or yarn workspaces)
# then individual package-lock.json or yarn.lock files in subdirectories might be ignored.
# Example for pnpm:
# pnpm-lock.yaml (at root, should be committed)
# **/node_modules

# Zip files (unless specifically intended for version control)
*.zip

